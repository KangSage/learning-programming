import React from 'react';
import ReactDOM from 'react-dom/client';
import element from '@/02.JSX/03_JSX에 표현식 포함하기 2';

ReactDOM.createRoot(document.getElementById('root') as HTMLElement).render(
  element,
);
